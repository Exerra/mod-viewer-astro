---
export interface Props {
    class?: string;
}

const classProp = Astro.props.class
---

<div class={`p-10 bg-white rounded-[24px] grow basis-1/2 2xl:basis-1/3 flex flex-col items-stretch ${classProp ?? ""}`}>
    <slot />
</div>